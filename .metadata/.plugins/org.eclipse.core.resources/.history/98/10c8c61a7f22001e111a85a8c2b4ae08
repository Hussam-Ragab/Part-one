/*
 *  Write a program to reverse a number.
 *
 * */

#include <stdio.h>

int main(void)
{
	int num,reverse = 0,remainder;
	printf("enter number\n");
	fflush(stdout);
	scanf("%d",&num);
	while(num != 0)
	{
		remainder = num%10;
		num /= 10;
		reverse *= 10 + remainder
	}

	return 0;
}
